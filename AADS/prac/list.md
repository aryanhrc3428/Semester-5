# "Practical List of AADS"
## Sheet - 1
1. Write a program to implement following vector operations: at(i), insert (i,e), erase(i,e), set(i,e), push_back(), pop_back().
2. Write a cpp program to implement a vector and perform extended array (Insertion,  deletion) operations using vector class.
3. list after k rotations
4. Given a vector of integers and a target value, return the indices of two numbers whose sum equals the target (2Sum).
5. Write a C++ program to remove duplicate elements from a sorted vector of integers.
## Sheet - 2
1. Implement a push function. Start with an array size of 2. If a student pushes an item into a full stack, create a new array that is double the size. Copy all old items over.
2. Track the exact number of operations. For a normal push, the cost is 1. For a resizing push, the cost is 1 plus the number of copied items.
3. Push 1,000 items in a loop. Print out the total cost and the average cost per push at the very end.
4.  Change the resizing rule. Instead of doubling, increase the size by adding +10 slots each time it fills up. Run 1,000 pushes again. Compare the average costs of both methods.
5.  Given a sorted vector, remove duplicates in-place and return the number of unique elements.
6.  Find the common elements between two vectors.
7.  Given a vector of integers, create two vectors: one containing even numbers and another containing odd numbers.
8.  Count how many times each element occurs in a vector.

## Sheet - 3
1. Given a sorted vector, remove duplicates in-place and return the number of unique elements.
2. Find the common elements between two vectors.
3. Given a vector of integers, create two vectors: one containing even numbers and another containing odd numbers.
4. Count how many times each element occurs in a vector.

## Sheet - 4
1. Implement a singly linked list and perform the following:
   - Insert a node at the beginning, end, and at a given position
   - Delete a node from the beginning, end, and a given position
   - Traverse and print the list
2. Implement a sequence using a linked list. Write functions to:
    - Insert at the front
    - Insert at the end
    - Reverse the sequence
3. Write a program to find the longest increasing subsequence (LIS) of a given sequence.
4. Given a sequence of numbers, implement a program to check whether it is a palindrome sequence or not.
5. Write a program to merge two sorted sequences into a single sorted sequence.
6. Write a program to implement a sequence using an array. Perform the following operations:
    - Insert an element at a given position
    - Delete an element at a given position
    - Find the length of the sequence
7. Implement a doubly linked list with insert, delete, and traverse operations.
   - Write a program to concatenate two linked lists.
   - Implement a program to split a circular linked list into two halves.
   - Create a program to detect and remove a cycle in a linked list.
8. Write a program to concatenate two linked lists.
9. Implement a program to split a circular linked list into two halves.
10. Create a program to detect and remove a cycle in a linked list.
11. Implement a hash table using division method for hashing and resolve collisions using linear probing.
12. Implement a hash table with quadratic probing as the collision resolution technique.
13. Write a program to implement hashing using chaining with linked lists.
14. Given a set of strings, store them in a hash table using a suitable hash function and search for a key.
15. Implement a hash function for student records (roll number, name) and perform insert, search, and delete operations.
16. Write a program to demonstrate the problem of primary clustering in linear probing and solve it using double hashing.

## Sheet - 5
1. Write a program that accepts a text string T of length n and a pattern string P of length m. Find all starting indices where P matches. Your program must maintain and output a global counter tracking the total number of character comparisons made during execution.
Understand the performance boundaries and O(n m) complexity of Brute Force.

2. Write a standalone function that takes a pattern string P and constructs its Longest Prefix which is also a Suffix (LPS) array.

## Sheet - 6
1. Write a program to implement KMP string pattern matching algorithm, output must include total number of comparisons made during execution.